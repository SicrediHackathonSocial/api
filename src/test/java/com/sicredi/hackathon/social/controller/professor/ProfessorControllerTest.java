package com.sicredi.hackathon.social.controller.professor;

//@RunWith(MockitoJUnitRunner.class)
//TODO finalizar testes desta classe
public class ProfessorControllerTest {

//    @MockBean
//    private ProfessorService professorService;


    //@Test
    public void post_cadastrar_professor() throws Exception {

//        final CadastrarProfessorRequest expected = CadastrarProfessorRequestFixture.get().random().build();

//        performPost("", expected);

//        final ArgumentCaptor<CadastrarProfessorRequest> captor = ArgumentCaptor.forClass(CadastrarProfessorRequest.class);
//        verify(professorService, times(1)).cadastrar(captor.capture());

//        assertEquals(expected.getNome(), captor.getValue().getNome());
//        assertEquals(expected.getMatricula(), captor.getValue().getMatricula());

    }

    //    @Test
    public void get_consultar_professor() throws Exception {

//        String expected = randomAlphabetic(10);

//        performGet("/", matricula);

//        final ArgumentCaptor<String> captor = ArgumentCaptor.forClass(String.class);
//        verify(professorService, times(1)).consultar(captor.capture());
//
//        assertEquals(expected, captor.getValue());
    }

}
